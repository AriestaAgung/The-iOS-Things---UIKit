# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'The iOS Things' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'Periphery'
  # Pods for The iOS Things

  target 'The iOS ThingsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'The iOS ThingsUITests' do
    # Pods for testing
  end

end
